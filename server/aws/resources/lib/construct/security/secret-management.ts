import { Construct } from 'constructs';

/** Placeholder for Secrets Manager (Verifier certificates). */
export class SecretManagement extends Construct {
  constructor(scope: Construct, id: string) {
    super(scope, id);
  }
}
