import { Construct } from 'constructs';

/** Placeholder for KMS signing keys (Issuer / Authz / Verifier). */
export class KeyManagement extends Construct {
  constructor(scope: Construct, id: string) {
    super(scope, id);
  }
}
