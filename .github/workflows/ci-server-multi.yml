name: CI multi server

# server depends on issuer+verifier
on:
  pull_request:
    branches: ["main"]
    paths:
      - server/multi/**

jobs:
  core:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.11.0
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run format
        run: |
          pnpm --filter ...@trustknots/multi-server format

      - name: Run lint
        run: |
          pnpm --filter ...@trustknots/multi-server lint

      - name: Build
        run: pnpm --filter ...@trustknots/multi-server... build
