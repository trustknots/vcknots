name: CI server

# server depends on issuer+verifier
on:
  pull_request:
    branches: ["main", "feat/oid4vci-1.0", "feat/oid4vp-1.0"]
    paths:
      - server/single/**

jobs:
  core:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7.0.1

      - uses: pnpm/action-setup@v6.0.10
        with:
          run_install: false

      - uses: actions/setup-node@v6.4.0
        with:
          node-version-file: .nvmrc
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run format
        run: |
          pnpm --filter ...@trustknots/server format

      - name: Run lint
        run: |
          pnpm --filter ...@trustknots/server lint

      - name: Build
        run: pnpm --filter ...@trustknots/server... build
